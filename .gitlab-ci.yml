test:
  image: docker:latest
  stage: build
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - echo "Hello, World! user $CI_REGISTRY_USER registry $CI_REGISTRY"

build-fake-sensors:
  image: docker:latest
  stage: build
  script:
    - echo "Building Fake Sensor Image"
    - docker build -t $CI_REGISTRY/iotssg5ws2020/fake-sensor:$CI_COMMIT_REF_SLUG ./fake-sensor/
    - docker push $CI_REGISTRY/iotssg5ws2020/fake-sensor:$CI_COMMIT_REF_SLUG
    # TODO push to docker hub